# Local Development Environment Configuration
NODE_ENV=dev

# Database URLs (local PostgreSQL without SSL)
# DATABASE_URL="postgres://edge_dev:edge_pass123@localhost:5432/nuri_db_check"

DB_HOST=localhost
DB_PORT=5432
DB_USER=edge_dev
DB_PASSWORD=edge_pass123
DB_NAME=nuri_db_check

# SSL Configuration (disabled for local development)
DB_SSL=false
DB_SSL_CERT_PATH=ssl/rds-ca-2019-root.pem
DB_SSL_REJECT_UNAUTHORIZED=false


# Clerk Configuration (for local testing)
# CLERK_ORG_ID="org_test_local_123"

# Migration Configuration
MIGRATION_BATCH_SIZE=2
MAX_RETRY_ATTEMPTS=3
MIGRATION_TIMEOUT=300000
